# Welcome to MkDocs

For full documentation visit [mkdocs.org](https://www.mkdocs.org).

## Commands

* `mkdocs new [dir-name]` - Create a new project.
* `mkdocs serve` - Start the live-reloading docs server.
* `mkdocs build` - Build the documentation site.
* `mkdocs -h` - Print help message and exit.
* `mkdocs gh-deploy` - 

## Project layout

    mkdocs.yml    # The configuration file.
    docs/
        index.md  # The documentation homepage.
        ...       # Other markdown pages, images and other files.

### for use in this project
1. Navigate to the `GHPagesData` folder via the command prompt
2. If you want to preview the site as you're editing run `mkdocs serve` and go to `localhost:8000` in browser.
3. Finally, when ready to push to github, run `mkdocs gh-deploy`.